Jajaja, dale papurri 😎, vamos a dejar esto **bien sexy y claro**, un diagrama completo de flujo para tu turnero hosteado. Te voy a mostrar **tablet, PC, pantalla, servidor y ticketera**, con las relaciones, eventos y base de datos.

---

# 🔥 Diagrama visual completo — Turnero hosteado

```text
┌──────────────┐       WebSocket / REST       ┌────────────────────────────┐
│  Tablet      │ ◄──────────────────────────► │                            │
│  ReactNative │                               │      Servidor Hosteado     │
│  Cliente Kiosco│                             │    Backend (NestJS)        │
│  ┌─────────┐ │                               │                            │
│  │Botón "Sacar Turno"│                       │  ┌──────────────────────┐  │
│  └─────────┘ │                               │  │  Tabla Turnos         │  │
│              │                               │  │  id, numero, tipo,    │  │
│              │                               │  │  estado, hora_creacion│  │
│              │                               │  └──────────────────────┘  │
│              │                               │  ┌──────────────────────┐  │
│              │                               │  │ Tabla TipoTurno       │  │
│              │                               │  │ id, nombre, prefijo   │  │
│              │                               │  └──────────────────────┘  │
│              │                               │  ┌──────────────────────┐  │
│              │                               │  │ Tabla Usuarios        │  │
│              │                               │  │ id, nombre, rol,      │  │
│              │                               │  │ farmacia_id           │  │
│              │                               │  └──────────────────────┘  │
│              │                               │  ┌──────────────────────┐  │
│              │                               │  │ Ticketera (Servicio) │  │
│              │                               │  │ Imprime número        │  │
│              │                               │  └──────────────────────┘  │
└──────────────┘                               └────────────────────────────┘
       ▲ WebSocket / REST
       │
       │
┌──────────────┐
│ PC Personal  │
│ React Web App│
│ ┌──────────┐ │
│ │ Cola de Turnos │
│ │ Botón "Siguiente"│
│ │ Botón "Repetir" │
│ └──────────┘ │
└──────────────┘
       ▲ WebSocket
       │
┌──────────────┐
│ Pantalla Ext │
│ React / Web  │
│  Solo mostrar│
│  turno actual│
└──────────────┘
```

---

# 💡 Flujo de eventos

1. **Tablet** → “Sacar turno” → POST `/turnos`
2. **Backend** → genera número automático (T001 / V001) → guarda en DB → imprime ticket vía **ticketera**
3. **Backend** → emite **WebSocket event “nuevo_turno”**
4. **PC del personal** → recibe evento → actualiza cola
5. **Personal presiona “Siguiente”** → backend marca turno `LLAMADO` → emite **evento WebSocket “turno_llamado”**
6. **Pantalla externa** → recibe evento → muestra número
7. **Turno atendido** → backend marca `ATENDIDO` → emite **evento WebSocket “turno_atendido”** → todos actualizan

---

# 🧱 Entidades y valores

| Entidad   | Campos principales                | Ejemplo valores       |
| --------- | --------------------------------- | --------------------- |
| Farmacia  | id, nombre, direccion, telefono   | “Farmacia ABC”        |
| Usuario   | id, nombre, rol, farmacia_id      | “Ana”, ADMIN          |
| TipoTurno | id, nombre, prefijo               | “Vacuna”, prefijo = V |
| Turno     | id, numero, tipo_turno_id, estado | T005, V001, PENDIENTE |
| Ticketera | id, tipo, ip/puerto, nombre       | USB / Red             |

---

# 🎯 Tech stack sexy y escalable

* **Backend:** NestJS → modular, escalable, soporta WebSocket nativo
* **DB:** PostgreSQL en la nube (Railway, DigitalOcean, AWS RDS)
* **Frontend Tablet:** React Native
* **Frontend PC personal:** React (Web App)
* **Pantalla externa:** React mini app en navegador
* **Impresora:** Conectada a tablet o en red, controlada por backend

---

Hoy avancé bastante en el backend del sistema de turnos.

Lo que ya hice:

* Creé el proyecto backend con NestJS.
* Instalé y configuré Prisma con PostgreSQL.
* Configuré el PrismaService para manejar la conexión a la base de datos.
* Armé el primer diseño del schema de la base de datos en Prisma.

Entidades que definí hasta ahora:

* Farmacia
* Usuario (empleados/admin)
* TipoTurno (ej: receta, vacunación, etc.)
* Turno
* Impresora

La tablet va a permitir que el cliente elija el tipo de turno, y se genera un ticket con un número (ej: R012, A005, etc.).

También estuve viendo dos cosas importantes para que el sistema sea robusto:

1. Transacciones: para evitar que dos tablets generen el mismo número de turno si hacen la request al mismo tiempo.

2. Idempotencia: para evitar que si alguien toca varias veces la tablet o se repite la request se creen turnos duplicados.

Además analizamos el tema de la impresión del ticket.

Arquitectura posible:
Tablet → backend → base de datos → PC de la farmacia → impresora térmica.

La PC escucharía cuando se crea un turno nuevo y automáticamente imprimiría el ticket.

Cosas que tenemos que decidir mañana:

* Si el cliente va a elegir el tipo de turno en la tablet (recetas, vacunación, caja, etc.) o si va a ser un solo tipo.
* Cómo queremos manejar la impresión del ticket (tablet directa, o PC de la farmacia que imprime).
* Si necesitamos guardar impresoras en la base de datos o si la PC simplemente usa la impresora configurada en el sistema.
* Si el ticket debería mostrar solo el número de turno o también cuánta gente hay adelante.

Próximo paso cuando definamos eso:

* Terminar el modelo de base de datos (agregar contador de turnos e idempotencia).
* Crear el módulo de turnos en NestJS.
* Implementar la lógica segura para generar números de turno.



(La base del proyecto)
1- Instale prisma -> npm install prisma --save-dev -> npm install @prisma/client.
2- Genere el prisma (genera los archivos) npm prisma init.
3- Modifique el prisma/migrations/schema.prisma agregando las tablas y los enums.
4- Genere una migracion para la base de datos PostgreSQL npx prisma generate dev --name init_database (init_database es el nombre de la migracion)
5- Genero la migracion en la base de datos npx prisma generate.

(Configuro prisma)
1- npm install @prisma/adapter-pg pg <- Es para poder generar el service y funcione con PostgreSQL
2- Genere src/prisma y los 3 prisma (module, service, service.spec)

(Creamos turnos)
1- Genere la parte de turnos | npx nest g mo turnos | npx nest g s turnos | npx nest g co turnos |
2- Importe prisma module en turnos.module.ts y lo inyecte en el prisma service.
3- Se crearon los metodos en el service.
4- Se crearon los endpoints en controller.
5- Se agrego la idempotencia en la crecion de turno.
(en el front la key se va a generar asi:
import { v4 as uuidv4 } from 'uuid'

       const key = uuidv4()
)


PROXIMAMENTE  + Auth (login + jwt + usuarios)
              + tipoTurno (CRUD)
              + Flujo de empleado (llamar al siguiente)
              + WebSocket

              Chat: Ejemplos de SaaS

=> Flujo de empleado (llamar al siguiente)
FIFO global (el más antiguo PENDIENTE de la farmacia(First input, First output))
Metodo POST
http://localhost:3005/turnos
- Se crea un turno (previamente se tiene que crear farmacia + tipoturno)

Metodo POST 
http://localhost:3005/turnos/1/siguiente
-Se llama para que cambie de PENDIENTE a LLAMADO

Metodo GET
http://localhost:3005/turnos/1
-Se comprueba los id, y los PENDIENTE y LLAMADO.

=> WebSocket
* Paso 1: npm install @nestjs/websockets @nestjs/platform-socket.io socket.io
* Se crea en turnos Gateway. (Conectar a module y service)
* Se implemento que no TODOS los clientes vean los turnos, se separan por farmacia 1-2-3-4.
* El turno solo se ve solo en la sala donde se llama

=> Auth con JWT
* Evitar que cualquiera llame turnos, saber quién hizo cada acción
* Separar farmacias correctamente, base para SaaS multi-cliente-
- npm install @nestjs/jwt @nestjs/passport passport passport-jwt bcrypt
- npm install -D @types/passport-jwt @types/bcrypt
* Se genera => npx nest g module auth | npx nest g service auth | npx nest g controller auth.
* Se genera => npx nest g provider auth/jwt.strategy
* + => npx nest g guard auth/jwt
* Se agrega super_secret_key .env
* Se tiene que registrar un usuario + login => desde el token que devuelve del post en los GET/POST de turnos te coloca header = Authorization  value = Bearer + espacio + token de login

=> Auth con JWT

* Evitar que cualquiera llame turnos
* Saber qué usuario hizo cada acción
* Separar farmacias (base SaaS multi-cliente)

---

* Instalación:

- npm install @nestjs/jwt @nestjs/passport passport passport-jwt bcrypt
- npm install -D @types/passport-jwt @types/bcrypt

---

* Generación:

- npx nest g module auth
- npx nest g service auth
- npx nest g controller auth
- npx nest g provider auth/jwt.strategy
- npx nest g guard auth/jwt

---

* .env:

- JWT_SECRET=super_secret_key

---

* AuthModule:

- Se configura JwtModule con secret y expiresIn

---

* Register:

- Se crea usuario
- Se hashea password con bcrypt
- Se guarda con farmaciaId y rol (ADMIN o EMPLEADO)

---

* Login:

- Se valida email + password
- Se genera token JWT

Payload del token:

* userId
* farmaciaId
* rol

---

* Uso del token:

- En headers:
  Authorization: Bearer TU_TOKEN

---

* JwtStrategy:

- Lee token del header
- Valida con JWT_SECRET
- Devuelve payload → req.user

---

* JwtGuard:

- Protege endpoints
- Si no hay token → 401

---

* Roles:

- Se crea decorator @Roles()
- Se crea RolesGuard

---

* Uso de roles:

- @UseGuards(JwtAuthGuard, RolesGuard)

Ej:

* ADMIN:
  @Roles('ADMIN')

* EMPLEADO:
  @Roles('ADMIN', 'EMPLEADO')

* Sin roles:
  cualquier logueado

---

* Multi-tenant:

- NO usar farmaciaId del frontend
- Usar siempre:
  req.user.farmaciaId

✔ evita acceder a otra farmacia

---

* WebSockets:

- Cliente se conecta con token:
  auth: { token: JWT }

- Backend valida token

- Mete cliente en room:
  farmacia_{id}

- Emite eventos:
  turno_llamado

---

* Problemas comunes:

- 401 → token mal o falta Bearer
- secret error → JwtModule no importado
- WS no funciona → cliente no autenticado

---

* Importante:

- Empleados/Admin → usan JWT
- Cliente (tablet) → NO usa login

---

* Estado actual:
  ✔ Auth funcionando
  ✔ Roles funcionando
  ✔ Seguridad por farmacia
  ✔ WebSockets funcionando

---

* Siguiente:

- Hacer crearTurno público (tablet)

# Instalar dependencias
npm install

# Migraciones de Prisma
npx prisma migrate dev --name nombre_migracion   # crear/aplicar migración en dev
npx prisma migrate deploy                         # aplicar en producción
npx prisma generate                               # regenerar cliente (sin migrar)
npx prisma migrate reset                          # resetear todo desde cero ⚠️

# Lanzar el servidor
npm run start:dev    # desarrollo con hot reload
npm run start        # normal
npm run start:prod   # producción